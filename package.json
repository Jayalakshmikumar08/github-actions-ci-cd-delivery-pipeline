{
  "name": "github-actions-ci-cd-delivery-pipeline",
  "version": "1.0.0",
  "description": "Production-style GitHub Actions CI/CD blueprint with commit quality, build, test and delivery stages.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node src/server.js",
    "lint": "node --check src/pipelineStatus.js && node --check src/server.js && node --check test/pipelineStatus.test.js && node --check test/server.test.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
